# OkamaOS inittab — console-grade boot, no login prompt in normal mode.
#
# Format: <id>:<runlevels>:<action>:<process>

# Mount filesystems and run /etc/init.d/* via BusyBox sysv compat.
::sysinit:/etc/init.d/rcS

# tty1: OkamaOS graphical shell — BusyBox init opens tty1 and execs the
# launcher directly (no openvt/chvt needed).  Respawns automatically on exit.
tty1::respawn:/usr/bin/okama-shell-launcher

# tty2: emergency/developer console
tty2::respawn:/sbin/getty -L tty2 0 vt100

# Stuff to do before rebooting
::shutdown:/etc/init.d/rcK
::shutdown:/sbin/swapoff -a
::shutdown:/bin/umount -a -r
::ctrlaltdel:/sbin/reboot
