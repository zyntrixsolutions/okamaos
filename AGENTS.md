# Repository Instructions

- Before any file updates, create a new git branch.
- After updates, commit and publish the branch.
- Always update `CHANGELOG.md` and control/track versions through `VERSION` and package `VERSION` files.
- Always track features and implementations in `ROADMAP.md` and update milestone checkboxes when milestones are implemented.
